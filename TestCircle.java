/*
public class TestCircle {
    public static void main(String[] args) {
        Circle circle1 = new Circle(); // First Object Creation via Circle CLASS and NO-ARG CONSTRUCTOR
        System.out.println("The Area of the First Circle of radius " + circle1.radius + " is " + circle1.getArea());
        Circle circle2 = new Circle(25); // Second Object Creation via Circle CLASS and SECOND CONSTRUCTOR
        System.out.println("The Area of the Second Circle of radius " + circle2.radius + " is " + circle2.getArea());
        Circle circle3 = new Circle(125); // Third Object Creation via Circle CLASS and SECOND CONSTRUCTOR
        System.out.println("The Area of the Third Circle of radius " + circle3.radius + " is " + circle3.getArea());
        circle2.radius = 100; //Setting new radius for Second Object
        System.out.println("The Area of the Second Circle of radius " + circle2.radius + " is " + circle2.getArea());
    }
}
//Defining Circle Class with two constructors
class Circle {
    double radius;
    //FIRST NO-ARG CONSTRUCTOR with radius 1
    Circle() {
        radius = 1;
    }
    //SECOND CONSTRUCTOR to build a circle with a specified radius
    Circle(double newRadius) {
        radius = newRadius;
    }
    //Method for Area Calculation
    double getArea() {
        return radius * radius * Math.PI;
    }
    //Method for Perimeter Calculation
    double getPerimeter(){
        return 2*radius*Math.PI;
    }
    //Method for setting Radius
    void setRadius(double newRadius){
        radius = newRadius;
    }
}*/
